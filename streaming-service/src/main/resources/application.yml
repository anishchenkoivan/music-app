minio:
  url: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: audio

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB